<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================ -->
    <!-- Permissions                                                  -->
    <!-- ============================================================ -->

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android 13+ runtime notification permission (required to post notifications) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Reschedule WorkManager jobs after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground service for the Accessibility-backed detection heartbeat.
         dataSync is the closest Play-approved type for short-lived sync work. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Usage Access (declared via signature|privileged; users grant via Settings).
         Listed so the Settings deep-link surfaces and Data Safety reflects the claim. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Vibrate for notification feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Explicitly REMOVED: QUERY_ALL_PACKAGES (Play policy violation). -->
    <!-- We rely on a narrow <queries> allowlist below instead. -->

    <!-- ============================================================ -->
    <!-- Package visibility allowlist (Android 11+, replaces           -->
    <!-- QUERY_ALL_PACKAGES). Limited to apps Unloop legitimately      -->
    <!-- detects for reels / short-form content tracking.              -->
    <!-- ============================================================ -->
    <queries>
        <package android:name="com.instagram.android" />
        <package android:name="com.zhiliaoapp.musically" /> <!-- TikTok global -->
        <package android:name="com.ss.android.ugc.trill" /> <!-- TikTok SEA -->
        <package android:name="com.google.android.youtube" />
        <package android:name="com.facebook.katana" />
        <package android:name="com.snapchat.android" />
        <package android:name="com.reddit.frontpage" />
    </queries>

    <application
        android:name=".UnloopApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="33">

        <!-- Crashlytics: collection is toggled at runtime in UnloopApplication
             (disabled in debug, enabled in release). NDK crash capture is
             enabled by the crashlytics-ndk artifact. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />


        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ============================================================ -->
        <!-- Accessibility Service (reels / short-form detection).        -->
        <!-- Disclosure flow shown in-app via AccessibilityDisclosure.    -->
        <!-- ============================================================ -->
        <service
            android:name=".accessibility.UnloopAccessibilityService"
            android:label="@string/accessibility_service_label"
            android:description="@string/accessibility_service_description"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Reschedule reminders after device reboot -->
        <receiver
            android:name=".reminders.BootCompletedReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>
</manifest>
